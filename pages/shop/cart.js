import React from 'react';
import Layout from '../../components/Layout/Layout';

const cart = () => {
    return (
      <Layout>
        <h2>This is cart.</h2>
      </Layout>
    );
};

export default cart;